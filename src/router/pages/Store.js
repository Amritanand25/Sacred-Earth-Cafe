import React from 'react';
import UILayout from '../../components/UILayout';
import StoreContainer from '../../components/Store/StoreContainer';

const Store = () => {
  return (
    <div>
        <UILayout Component={StoreContainer} />
    </div>
  )
}

export default Store